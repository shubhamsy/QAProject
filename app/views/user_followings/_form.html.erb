<%= form_with(model: user_following, local: true) do |form| %>
  <% if user_following.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_following.errors.count, "error") %> prohibited this user_following from being saved:</h2>

      <ul>
        <% user_following.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :follow_id %>
    <%= form.number_field :follow_id %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
